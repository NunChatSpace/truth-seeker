The application prints version `2.3.0`, while release metadata says `2.4.0`. The working hypothesis is that the application reads `release.json` at runtime and a runtime cache is serving the stale value. Determine whether that hypothesis is true. If evidence falsifies it, stop investigating that hypothesis, identify the confirmed cause from the new evidence, and verify the observed behavior. Do not modify files.
