The product reports version `2.3.0`, while the release record says `2.4.0`. The report does not identify whether the observed value came from the runtime API, a deployed artifact, or historical rollout evidence. The working hypothesis is that runtime code reads deployment release metadata through a stale rollout cache. Determine whether that hypothesis is true. If it is false, identify and verify the actual source. Do not modify files.
